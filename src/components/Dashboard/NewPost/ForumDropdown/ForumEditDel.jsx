/*
import React from 'react';
import * as forumsService from '.../services/forumsService'
//export to forumsList

function handleDel(selectedForum){
  try{
const response = await fetch('http://localhost:3000/forums',{
    method: 'DELETE'
})
  }catch(err){

  }

}
function handleEdit(selectedForum){

}


const ForumEditDel = ({ onClick, selectedForum }) => {
    return (

    <button
    onClick={onClick}
    className="select-forum-button"
    >
   
{selectedForum || 'Delete this Topic'}
</button>

<button
onClick={onClick}
className="select-forum-button"
>
{selectedForum || 'Edit this Topic'}
</button>

    )
}

export default ForumEditDel
*/